# Next.js rewrites(next.config.ts)가 서버 사이드에서 읽어
# "/api/:path*" 요청을 이 주소로 프록시합니다. 브라우저에는 노출되지 않으므로
# NEXT_PUBLIC_ 접두사를 붙이지 않습니다.
#
# 로컬 개발 시 backend(meeting-api)를 localhost:8080에서 실행한다는 가정입니다.
API_SERVER_URL=http://localhost:8080
