services:
  meeting-web:
    build:
      context: .
    container_name: meeting-web
    ports:
      - "3000:3000"
    environment:
      - API_SERVER_URL=${API_SERVER_URL:-http://localhost:8080}
    restart: unless-stopped
